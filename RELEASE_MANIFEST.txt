LEAD PLATFORM — ADMIN / PRODUCTION / PERFORMANCE BUNDLE

PURPOSE
- Add production operations, admin control, durable workers and a deeper performance pass without redesigning the working lead/outreach product.

MAJOR ADDITIONS
- backend/migrations/004_admin_operations.sql
- backend/app/admin/
- backend/app/api/admin.py
- backend/app/jobs/worker.py
- frontend/src/pages/AdminPage.tsx
- production render.yaml
- lower-cost render-low-cost.yaml
- snapshot APIs for My Leads and Outreach
- persistent Turso HTTP connection pooling
- user access cache + frontend in-flight request dedupe
- Hostinger live-webhook status/setup UI

KEEP / NEVER OVERWRITE WITH THE ZIP
- .git/
- backend/.env
- backend/.venv/
- frontend/.env
- frontend/package-lock.json
- frontend/node_modules/
- Firebase Admin private JSON
- CREDENTIAL_ENCRYPTION_KEY value
- Turso / provider / Hostinger secrets

DATABASE
- Take a Turso backup/branch before applying to important data.
- Run: python -m app.db.migrate
- Expected new migration: 004_admin_operations
- Run migration again; it should report up to date.

LOCAL ADMIN
- Add your own login email to backend/.env:
  ADMIN_EMAILS=you@example.com
- Restart backend and sign in again.

TEST
- Backend: pytest -q
- Frontend: npm run check && npm run test && npm run build
- Verify admin access, user controls, lead/outreach page speed, worker health and existing lead/outreach regression flows.

PRODUCTION
- Follow docs/PRODUCTION_DEPLOYMENT.md.

ROLLBACK
- Preserve the previous working Git commit before updating.
- Migration 004 is additive, but production data should still be backed up before applying it.
