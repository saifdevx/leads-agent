LEAD PLATFORM — DISCOVERY QUALITY & RELIABILITY UPDATE

Purpose:
- Improve automated lead accuracy before adding paid enrichment providers.
- Fix false phone/company fields and duplicate businesses.
- Make temporary Turso failures recover automatically.
- Harden AI provider security and fallback behavior.

No new frontend dependency.
No new Python dependency.
No database migration required.
No new environment variable required.

Recommended local environment adjustment:
- TURSO_TIMEOUT_SECONDS=15

Critical security action before reusing Gemini:
- Rotate the Gemini key that appeared in previous local logs.
- Replace the old Gemini key through Settings after installing this update.

Preserve locally:
- .git/
- backend/.env
- backend/.venv/
- frontend/.env
- frontend/package-lock.json
- frontend/node_modules/

Acceptance:
- Existing regression tests pass locally.
- Gemini key never appears in request URLs/logs.
- Temporary job-status 503 does not stop frontend polling.
- Facebook IDs/dates are not parsed as phone numbers.
- Explicit Florida results are rejected for a Texas search.
- Template/demo hosts do not count as leads.
- Same business/domain merges instead of creating duplicate rows.
- Serper automated search still completes and saves leads.
