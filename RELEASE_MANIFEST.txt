LEAD PLATFORM — REPLIES / FOLLOW-UPS / ANALYTICS BUNDLE

PURPOSE
- Add reply handling, follow-up automation, campaign analytics and faster UI interactions.

ADD / MAJOR CHANGES
- backend/migrations/003_replies_followups.sql
- backend reply/follow-up repository and API support
- Hostinger inbox sync + webhook-ready receiver
- frontend Reply Inbox + campaign detail drawer
- frontend optimistic actions + response cache
- bulk lead deletion
- subtle motion styles

KEEP / DO NOT REPLACE WITH SECRETS
- .git/
- backend/.env
- backend/.venv/
- frontend/.env
- local frontend/node_modules/
- CREDENTIAL_ENCRYPTION_KEY value
- Firebase Admin private JSON

DATABASE
- Run `python -m app.db.migrate` once after update.
- Expected new migration: 003_replies_followups.

TEST
- Backend: pytest -q
- Frontend: npm run check && npm run test && npm run build
- Then test one controlled send + reply sync + stop-on-reply.

ROLLBACK
- Keep the previous Git commit before copying this bundle.
- Because migration 003 restructures email_messages, take a Turso backup/branch before applying it to important production data.
