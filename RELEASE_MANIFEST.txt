LEAD GEN — FINAL RELEASE BUNDLE

PURPOSE
- Finalize the working Lead Gen product with branding, UI polish, performance improvements, security hardening and a default zero-cost Render deployment.

MAJOR ADDITIONS / CHANGES
- Lead Gen branding across frontend/backend/docs/exports/webhooks
- User-supplied logo assets, favicon, Apple touch icon and manifest
- polished sidebar, top bar, auth experience, forms, tables and motion
- empty default lead-search form (no demo data prefilled)
- automatic retry for transient GET failures
- one-round-trip authenticated-user Turso sync
- API gzip + security headers
- Quick Send rate limiting
- free Render Blueprint with embedded lead/outreach workers
- optional scaled Render Blueprint preserved as render-scaled.yaml
- final deployment and release-checklist docs

KEEP / NEVER OVERWRITE WITH THE ZIP
- .git/
- backend/.env
- backend/.venv/
- frontend/.env
- frontend/package-lock.json
- frontend/node_modules/
- Firebase Admin private JSON
- CREDENTIAL_ENCRYPTION_KEY value
- Turso / provider / Hostinger secrets

DATABASE
- No new schema migration was added in this final polish bundle.
- Run: python -m app.db.migrate
- It should report that the schema is already up to date after your existing migrations are applied.

LOCAL TEST
- Backend: pytest -q
- Frontend: npm run check && npm run test && npm run build
- Run one lead search, export, Hostinger Quick Send, campaign, reply test and Admin health check.

FREE DEPLOYMENT
- Use the default render.yaml.
- It creates a free Static Site plus one free Web Service.
- The API runs embedded lead/outreach workers while awake.
- Read docs/PRODUCTION_DEPLOYMENT.md for the free-tier sleep trade-off and exact environment variables.

ROLLBACK
- Preserve the previous working Git commit before replacing source files.
- No destructive migration is introduced by this bundle.
